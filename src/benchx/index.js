"use strict";

const { bench } = require("./bench");
const { compare } = require("./compare");
const { createRunner } = require("./create-runner");

module.exports = {
	bench,
	compare,
	createRunner,
};
